// PublisherItem.jsx

// import libraries
import React from 'react';

// Import Services
// Import components
// Import styles

const PublisherItem = ({ publisher }) => {
    return (
        <div>
            {publisher.name}
        </div>
    );
};

export default PublisherItem;
