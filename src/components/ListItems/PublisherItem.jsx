// PublisherItem.jsx
import React from 'react';

const PublisherItem = ({ publisher }) => {
    return (
        <div>
            {publisher.name}
        </div>
    );
};

export default PublisherItem;
